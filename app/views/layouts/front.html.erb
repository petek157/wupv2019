<!DOCTYPE html>
<html>
  <head>
    <title>Western UP Vacation</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://fonts.googleapis.com/css?family=Nunito|Permanent+Marker&display=swap" rel="stylesheet">
  </head>

  <body>
    <header>
    <div id="mobile-menu">
        <a href="#">Explore</a><!--business-->
        <a href="#">Stay</a><!--lodging system and just facilities, fly in (airport) -->
        <a href="#">Maps</a><!--Trails, hills, falls, snow totals/coverage etc-->
        <a href="#">Conditions</a><!--Trails, hills, falls(how much the falls are running), fall colors,  etc-->
        <%= @user.nil? ? link_to("Login", "#") : "Hello #{@user.first}" %>
    </div>
        <div class="container make-space">
            <a class="logo" href="<%= root_path()%>">Logo</a>
            <div class="mobile-menu-icon">Menu</div>
            
            <nav class="desk-menu">
                <ul>
                    <li class="menu-item"><a href="#">Explore</a></li><!--business-->
                    <li class="menu-item"><a href="#">Stay</a></li><!--lodging system and just facilities, fly in (airport) -->
                    <li class="menu-item"><a href="#">Maps</a></li><!--Trails, hills, falls, snow totals/coverage etc-->
                    <li class="menu-item"><a href="#">Conditions</a></li><!--Trails, hills, falls(how much the falls are running), fall colors,  etc-->
                </ul>
            </nav>
            <div id="user" class="desk-menu">
                <%= @user.nil? ? link_to("Login", "#") : "Hello #{@user.first}" %>
            </div>
        </div>
        <div class="nav-bottom-below-white"></div>
    </header>
    
    <div class="home-content">
        <%= yield %>
    </div>
    <footer>

    </footer>
  </body>
</html>